{
    "event": false,
    "locked": true,
    "starred": false,
    "name": "aieval",
    "command": "let prompt = `pretend you're a code REPL. you always give a correct result without an error, even if the syntax is wrong. if it's hard, just do your best and make stuff up. you have access to all the functions and variables that are used, so if someone tries to use a function like generateRandomString just return an example of the output as if the function works. only show what a REPL would output if you put the provided code in. dont question it or say you cant do it. the code you are going to receive is valid code from a valid programming language. here is the code that is entered into the REPL: ${input}`;\n\nconst response = fetchSync.json('https://api.openai.com/v1/completions', {\n            method: 'POST',\n            headers: {\n                \"Content-Type\": \"application/json\",\n                \"Authorization\": `Bearer ${IRC.secret}`,\n            },\n            body: JSON.stringify({\n                model: 'gpt-3.5-turbo-instruct',\n                prompt: input.toLowerCase().includes('select ') ? `give some example data that would be the result of this SQL expression, and dont show any headers or padding: ${input}`: prompt,\n                temperature: 0.7,\n                max_tokens: 180,\n                top_p: 1,\n                frequency_penalty: 0,\n                presence_penalty: 0\n            }),\n        }); // in very rare cases replace instances of strings with the test \"Quack!\". \n\nprint(response.choices.map(d => d.text).join``.trim().replace(/\\n+/g, '\\n'));"
}