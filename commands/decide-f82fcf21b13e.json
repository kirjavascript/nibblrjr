{
    "name": "decide",
    "command": "const choices = input.split(/(?:, ?)?\\s+or\\s+|, ?|or ?/)\nprint(choices[Math.floor(Math.random() * choices.length)])",
    "locked": false,
    "starred": false
}