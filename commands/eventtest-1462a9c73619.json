{
    "event": false,
    "locked": true,
    "starred": false,
    "name": "eventtest",
    "command": "const sd = jsdom()\n\n\nIRC.listen('tick', (events) => {\n  //print('the time is ' + new Date);\n});\n\nIRC.listen('message', event => {\n\tif (event.message.text.includes('foo')) {\n       IRC.require('8ball')\n    }\n  \tif (event.message.text === 'oh no') {\n       sleep(1); print('oh no');\n    }\n})"
}