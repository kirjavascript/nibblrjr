{
    "event": false,
    "locked": true,
    "starred": false,
    "name": "eventtest",
    "command": "\n\n\nIRC.listen('tick', (events) => {\n  //print('the time is ' + new Date);\n  global.input = (new Date).toISOString();\n  //print(1)\n  //IRC.require('8ball');\n  //log(IRC.queryConfig('password'))\n}, { showError: 1 });\n\nIRC.listen('message', event => {\n\tif (event.message.text.includes('foo')) {\n    print(IRC.require('8ball'))\n    }\n  \tif (event.message.text === 'oh no') {\n       print('oh no');\n    }\n})"
}