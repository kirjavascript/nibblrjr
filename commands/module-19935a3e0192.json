{
    "name": "module",
    "command": "const commands = IRC.commandFns.names().filter(d => d.startsWith(IRC.command.list[0] + '.')).map(d => d.split('.').slice(1).join('.'));\nprint(`all subcommands: ` + commands.map(d=>`{u}${d}{/}`).join` `)",
    "locked": true,
    "starred": true
}