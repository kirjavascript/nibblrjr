{
    "event": false,
    "locked": true,
    "starred": true,
    "name": "reload",
    "command": "const isVM = input.trim() === 'vm';\nIRC.sudo().node.parent[isVM ? 'reloadVM' : 'reloadEvents'].call();\nprint.success('reloading '+(isVM?'vm':'events'));"
}