{
    "locked": true,
    "starred": false,
    "name": "repo",
    "command": "input = input.trim() || IRC.message.from;\nconst self = input == IRC.message.from;\nconst repo = store.get(input.toLowerCase());\nprint(repo ? repo.split` `.map(d => IRC.colors.link(d)).join` ` : `no repo saved for ${IRC.colors.nick(input)} `  + (self ? `add one with ${IRC.colors.cmd('repo.set', 'URL')}` : ''))"
}