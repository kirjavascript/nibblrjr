{
    "name": "roll7x",
    "command": "// optimized for speed \nlet sevens = 0;\nconst qty = eval(input) || 1;\nlet i = 0;\nwhile(++i<=qty) {\n    if (\n        (0|Math.random()*6) === 0 &&\n        (0|Math.random()*6) === 0 &&\n        (0|Math.random()*6) === 0 &&\n        (0|Math.random()*6) === 0 &&\n        (0|Math.random()*6) === 0 &&\n        (0|Math.random()*6) === 0 &&\n        (0|Math.random()*6) === 0\n    ) {\n        sevens += 1;\n    }\n}\nprint(`{o}${sevens}`);\n",
    "locked": true,
    "starred": false
}