{
    "name": "shuffle",
    "command": "print.raw(( // legacy command wrapper\n   function shuffle(q) {o=[...q];for(let j, x, i = o.length; i; j = Math.floor(Math.random() * i), x = o[--i], o[i] = o[j], o[j] = x);return o.join('')}\n)(input, input.split(' '), IRC.message));",
    "locked": false,
    "starred": false
}