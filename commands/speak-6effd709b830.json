{
    "name": "speak",
    "command": "const _ = require('lodash');print(_.shuffle(IRC.log.random(1+(new Date%2)).map(d => d.message).join` `.split` `).join` `)",
    "locked": true,
    "starred": false
}
