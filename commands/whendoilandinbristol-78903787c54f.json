{
    "locked": false,
    "starred": false,
    "name": "whendoilandinbristol",
    "command": "var arrival = new Date(); arrival.setHours(22); arrival.setMinutes(15); var departure = new Date(Date.now()); var diff = (arrival.getTime() - departure.getTime())/(1000*60*60); print(`Arrival in Weston in ${Math.floor(diff)} hours and ${Math.floor((diff - Math.floor(diff))*60)} minutes`);"
}