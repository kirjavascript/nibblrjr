// TODO //

    poker(query, text) { store.'running' poker.reset
    Buy in is ... bet or fold
    august fools

====

disable commands
strip only starred commands for github ?
access part/join/kick/nick - just have 'admins' in config

> limit store set amount to 200 characters
> .bots standard
> acquire - use cache instead of always checking the latest version
> create acquire cache directory if it doesn't exist
> cache versions are displayed as 1.1.1 for things that aren't at 1.1.1 for some reason
> fix fetch/localStorage
> reauth on ~nick change
> stop bulk command editing
pass command parsing in, replace in command.random and repeat
add breakhighlight to api
amazon / soundcloud url parsing / set Accept-language header
provida toQueryString
redditVideoURL builtin

====

~poll event.tick
~pipe can still do async chaining by changing what print and getJSON etc do, getting commands to run and executing them in promises
~matrix(7)1 - command parse bug
~commands.*
~print(){r}hello
http://passpigs.tripod.com/prob.html
log.user
blackjack
getDOM('https://apps.timwhitlock.info/unicode/inspect?s='+encodeURIComponent('™')).then(dom => { print(dom.qs('#inspect tbody').textContent.replace(/\s+/g,' ')) }).catch(print.log);
https://github.com/4chan/4chan-API
trivia() { // http://opentdb.com/api.php?amount=10
roulette
mdn
memo.pending
ciu
https://stackoverflow.com/questions/29478751/how-to-cancel-an-emcascript6-vanilla-javascript-promise-chain
clearInterval

// WEB API
test command
test parse command
// dispatch message on print
// join new channels/servers
// track nick list

// UI
react router
// subcommands / tree
only web ui can kick/part/join
// db web backup zip
database explorer

// EDITOR
edited
testing

constants: '>'
