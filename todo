// TODO //

// CMD
// 2d3h time
// check valid command on the command line - suggest ~api
// ~command.new.force() command overwrite
// ~command.new.text
// check parent locked
// ~poker (do all in the api, one round a time, scoped stats)
// command piping return print();
Async (sync only?) promise chain pipe - test when commands are in UI  / only some are async functions and the rest are? sync/whatever
help ?
// hyperslots {g,r} watermelon ฿
// logs - profanity stats
// ~eval has moved to ~>, help.eval for more info ( ~help lists different helps (help, commands, format)
// alg.js
for setTimeout, create a list of timers - flush
// ~speak typeerror bofh

// DATABASES
// remind / memo

// WEB API
// dispatch message on print
// join new channels/servers
// track nick list
// look at old nibblr code to work out the environment
port via print(( () => { ... legacy code ... } )(args.legacy))
{emoji}

// UI
blueprint / react router / ace editor
// subcommands / tree
only web ui can kick/part/join
implement internal commands as
// db web backup zip

// EDITOR
// update legacy tool in web UI
// better editor - vim mode -
edited
testing

patch the rizon log

// http://www.df7cb.de/irc/pisg/pisg-month.html
https://www.npmjs.com/package/date.js
https://www.npmjs.com/package/timestring

// await getJSON.test.test example
// document web API / command context
// event backend
// about -> github iframe
// old prs
bully
wca
mdn
bridges?
