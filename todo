// TODO //

    poker(query, text) { store.'running' poker.reset

    slots() { megaslots() { slotstats() {
    // slots has been played N times
// hyperslots {g,r} watermelon ฿ hyperslots bigint
    slots.donate
?!bet 9999 heads
more things, but smaller bag each time

    // savely write
    // commands have write only access (still use --force for overwrite)
    help.log => log.about
    help.commands => commands.about
    commands.new
    commands.edit
    commands.text
    commands.search
    commands.random
    commands.source
    commands.starred
    view starred commands for better ifgo

    remind(query, text) {
    memo(query, text) { target: {bo}<{/}${IRC.parseColors.hashString(user)}${user}{/}{bo}>{/} message // add to util.memo
    memo.multi(,,)
    roll7
    trivia() { // http://opentdb.com/api.php?amount=10

https://github.com/4chan/4chan-API
// http://www.df7cb.de/irc/pisg/pisg-month.html
https://www.npmjs.com/package/date.js
https://www.npmjs.com/package/timestring

 help.colors help.logs help.memo help.games help.timestamps
memo/remind check IRC.command | 2d3h time

====


// command piping (alt print command, timeout wait etc)
cancelable promises getRequest (defend infinite loop

// WEB API
test command
test parse command
// dispatch message on print
// join new channels/servers
// track nick list

// UI
react router
// subcommands / tree
only web ui can kick/part/join
// db web backup zip

// EDITOR
edited
testing

patch the rizon log
fix uglify bug
~shreddydropsthenbomb

// alg.js
track nicklist
require() support | npmi
can still do async chaining by changing what print and getJSON etc do, getting commands to run and executing them in promises
bully
wca
mdn
bridges?
data:,<script> (function() { var cx = '004589196096329597303:kqsojvcceue'; var gcse = document.createElement('script'); gcse.type = 'text/javascript'; gcse.async = true; gcse.src = 'https://cse.google.com/cse.js?cx=' + cx; var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(gcse, s); })(); </script> <gcse:search></gcse:search>
