// TODO //

    weather(query, text) { african
~reddit # _.shuffle() qty
    google(query, text) {
    youtube(query, text) { site:youtube.com
    poker(query, text) {
    trivia() { // http://opentdb.com/api.php?amount=10
    clue() {
    skip() {
    triviastats() {
    slots() {
    megaslots() {
    slotstats() {
    define(words, text) {
    example(words, text) {
    imgur(query, text) {
    pornhub(query, text) {
    drug(query, text) {
    reddit(query, text) {
    speak(query, text) { // use log
    remind(query, text) {
    memo(query, text) { target: {bo}<{/}{rand}source{/}{bo}>{/} message // add to util.memo
    "log": function(query, text) { IRC.log
    log.regex
    seen(who, text) {
    "commands": function (query, text) {
    roll7

https://github.com/4chan/4chan-API

MVP: auth, log, help, slots, poker
help.commands help.colors (test with ~print) help.urls help.eval help.logs help.memo help.games help.timestamps
memo/remind check IRC.command | 2d3h time
// hyperslots {g,r} watermelon ฿ hyperslots bigint

delete -> sure?
sureDelete

command.new
command.edit
command.new.text
command.search
command.random
command.source
commands.starred
====


// command piping (alt print command, timeout wait etc)
cancelable promises getRequest (defend infinite loop
multiMemo(,,)

// WEB API
test command
test parse command
// dispatch message on print
// join new channels/servers
// track nick list

// UI
react router / ace editor
// subcommands / tree
only web ui can kick/part/join
// db web backup zip

// EDITOR
edited
testing

patch the rizon log

// http://www.df7cb.de/irc/pisg/pisg-month.html
https://www.npmjs.com/package/date.js
https://www.npmjs.com/package/timestring

// alg.js
track nicklist
roll 7 high scores (hiscore/loscore baleet)
require() support | npmi
can still do async chaining by changing what print and getJSON etc do, getting commands to run and executing them in promises
server config: starred commands only
// await getJSON.test.test example
// document web API / command context
// event backend
// about -> github iframe
// old prs
// ~speak typeerror bofh
bully
wca
mdn
bridges?
