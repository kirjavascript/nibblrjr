// TODO //

MVP: built in commands, memo/remind, command creation, auth, log, help, slots, poker
memo/remind check IRC.command

help.commands help.colors help.urls help.eval
// ~eval has moved to ~>, help.eval for more info
LOG: IRC.log

MEMO: // 2d3h time
// ~command.new.force() command overwrite
// ~command.new.text
search, random
~reddit #
~speak just uses IRC.log
// check parent locked
// ~poker (do all in the api, one round a time, scoped stats)
store.getDefault()
// hyperslots {g,r} watermelon ฿ hyperslots bigint
// logs - profanity stats
cancelable getRequest (defend infinite loop
cancelable listeners?
roll 7 high scores (hiscore/loscore baleet)

// WEB API
complete auth
test command
test parse command
// dispatch message on print
// join new channels/servers
// track nick list
// alg.js

// UI
react router / ace editor
// subcommands / tree
only web ui can kick/part/join
// db web backup zip

// EDITOR
edited
testing

patch the rizon log

require() support
can still do async chaining by changing what print and getJSON etc do, getting commands to run and executing them in promises

// http://www.df7cb.de/irc/pisg/pisg-month.html
https://www.npmjs.com/package/date.js
https://www.npmjs.com/package/timestring

// await getJSON.test.test example
// document web API / command context
// event backend
// about -> github iframe
// old prs
// ~speak typeerror bofh
bully
wca
mdn
bridges?
